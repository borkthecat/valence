services:
  gateway:
    env_file: .env.example
    environment:
      ENTERPRISE_INGEST_AUTH_MODE: api_key

  pipeline:
    env_file: .env.example
    environment:
      PROXY_HOST: gateway
      PROXY_PORT: "8080"
      MOCK_AI_PROVIDER: "true"
